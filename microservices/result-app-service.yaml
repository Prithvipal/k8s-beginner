apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name: result-service
    app: demo-voting-app
spec:
  type: NodePort
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30005
  selector:
    name: voting-result-pod
    app: demo-voting-app
    
